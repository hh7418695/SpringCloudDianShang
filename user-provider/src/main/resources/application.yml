server:
  port: 8601
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/b2bdata?useSSL=false&serverTimezone=Asia/Shanghai&&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: b2b-user-provider
eureka:
  client:
    service-url:
      defaultZone: http://root:123456@localhost:8500/eureka/
  instance:
    hostname: localhost
mybatis:
  mapper-locations: classpath:/mapper/*
